# Vocab Trainer — 語彙特化学習アプリ 機能計画 v1.5

場所: `c:\dev\01_Accenture\Vocab_Trainer\` ／ 作成: 2026-07-13
根拠: 7エージェント調査（データセット×3言語・学習方式・Teams連携・競合10製品・相互検証）。ライセンス・製品状況は一次ソースで検証済み。

> **v1.5 の変更（残り4コースの実行計画確定）**: コースC（日本語0→3k）出荷後、残り4コース（D/E/A/B）の実行計画を確定。**最重要の訂正**: 「コースB（タガログ語）は同一レールエンジンのデータ差し替えだけ」という v1.4 までの前提は誤りと判明——コース切替UI・コースごとの引用元表示・カバレッジ計算・多言語UI(i18n) が現状すべてコースC専用にべた書きされており、2コース目を出す時点で共通基盤（新設 **Phase 1.5**）が必要。加えて①コースC/D/E のグロス言語を英語→**タガログ語**へ変更（フィリピンチーム向け判断、2026-07-15）②cloze・較正エンジンは MVP を経ず PLAN §4.2/§4.3 仕様をフルスコープで最初から実装③コンテンツ生成（グロス・例文・カテゴリ分類・コーチ文）は人手ネイティブレビュー無し、複数AIの相互検証のみで品質担保（要リスク管理）。詳細は §1・§5.3・§7・§8(#14-19)・§9。
> **v1.4 の変更（音声不採用）**: 事前生成TTS音声を採用すると1万→3万語帯だけで GitHub Pages の1GB上限を単独で超過する試算（約1.33GB、§6）が出たため、**音声機能を無料のWeb Speech API版も含めて全面撤去**し、**発音表記**（日本語=かな／英語=IPA・CMUdict／タガログ語=アクセント付き表記）に一本化。データ量ゼロ・追加ホスティング不要で解決。詳細は §3.4・§4.1・§6・§8(#13)。
> **v1.3 の変更（サーバーレス確定）**: バックエンドを持たない **100% クライアントサイド（サーバーレス）** に確定。①スタックを **Vite 静的 SPA**（Next.js から変更）②配布は **個人 GitHub の public リポジトリ → GitHub Pages で無料公開**③進捗は端末内 IndexedDB＋**手動 JSON バックアップ**（サーバー同期なし）④public 配信のため**データ層を再配布可能なオープンデータのみに限定**（BCCWJ・jpdb 不使用、頻度は wordfreq 系に差し替え）。詳細は §5・§6・§8。
> **v1.2 の変更**: メンバー用日本語コースを並行スコープに復帰（「各メンバーが個人用ツールとして使う」＝チーム共有機能は不要だが利用者は複数）。プロジェクトを 01_Accenture 配下に配置。
> **フォルダ名 `Vocab_Trainer` は仮**。希望名があれば即リネーム可。

---

## TL;DR

- **1つの語彙学習エンジン × 複数コース**。学習ロジック（出題・復習・進捗）は全コース共通で、違いは語彙データと表示言語だけ
- **利用者は複数だが各自が個人用**：チームリーダーボード等の社会的機能は作らない。各人が自分の進捗だけを持つ
- 2系統5コース：
  - **Kohei 系**: 英語 10k→30k（配置テスト＋マイニング型）／タガログ語 0→3k（レール型）
  - **メンバー系**: 日本語 0→3k（出荷済み）・3k→10k・10k→30k（レール型→cloze→較正、JLPTバッジ付き）
  - コースC（日本語0→3k）出荷後は**直列**で1コースずつ着手（Phase2→3→4→5。並行案は2026-07-15に撤回。理由は §7・判断ログ#14）
- 復習エンジン = **FSRS-6**（Anki 採用の最新式。MIT の ts-fsrs で合法組込）。SRS は自作しない
- **Teams 連携 = 単語収穫**：チャットでやりとりした／分からなかった単語をアプリに取り込んで学習カード化。通知・順位表は作らない
- **サーバーレス**: バックエンド無し。Vite 静的 SPA を個人 GitHub → GitHub Pages で無料公開。進捗は端末内 IndexedDB＋手動 JSON バックアップ（クラウド同期なし）
- データは**3言語とも再配布可能なオープンデータのみで構築**（ライセンス一次ソース検証済み）。GitHub Pages 公開＝再配布のため研究/教育限定データ（BCCWJ・jpdb）は不使用

## 0. 確定事項（2026-07-13 Kohei 判断）

| 論点 | 決定 |
|---|---|
| 用途方針 | **社内・個人限定**（販売しない前提） |
| 利用者モデル | **複数人・各自が個人用**（Kohei＋フィリピンチーム各自）。チーム共有・比較機能は不要 |
| MVP 順序 | ~~並行（Kohei系＋メンバー系を並走）~~ → **コースC出荷後は直列**（Phase2→3→4→5。2026-07-15判断ログ#14で撤回・変更） |
| Teams 連携 | チャット単語のアプリ反映＋分からない単語の辞書引き。リマインダー/リーダーボードは作らない |
| **バックエンド** | **無し＝サーバーレス**（100% クライアントサイド。Supabase 等は不採用） |
| **配布/ホスティング** | 個人 GitHub の **public リポジトリ → GitHub Pages（無料）**。メンバーは URL を開くだけ・PWA 可 |
| **進捗の保存** | 端末内 **IndexedDB ＋ 手動 JSON バックアップ**（クラウド同期なし） |
| **データ層** | **再配布可能なオープンデータのみ**（public 配信のため BCCWJ・jpdb 不使用。頻度は wordfreq 系） |
| 配置場所 | `01_Accenture/Vocab_Trainer/` |

## 1. コース構成

| 系統 | コース | 学習言語 | グロス（訳語） | UI | 型 | 状態 |
|---|---|---|---|---|---|---|
| Kohei | A | 英語 10k→30k | 日本語 | 日本語 | 較正＋マイニング | 未着手（Phase 5） |
| Kohei | B | タガログ語 0→3k（**実質0→2k**。2026-07-15判断ログ#20） | 日本語（英語併記） | 日本語 | レール | **出荷済み**（コースID: `tl-0-2k`） |
| メンバー | C | 日本語 0→3k | **タガログ語**（2026-07-15変更） | 英語 | レール | **出荷済み** |
| メンバー | D | 日本語 3k→10k | **タガログ語**（2026-07-15変更） | 英語 | cloze主軸 | 未着手（Phase 3） |
| メンバー | E | 日本語 10k→30k | **タガログ語**（2026-07-15変更） | 英語 | 較正＋マイニング | 未着手（Phase 4） |
| メンバー | かな導入 | ひらがな・カタカナ計208字（清音46＋濁音/半濁音25＋拗音33を両文字体系分） | ローマ字 | 英語 | かな導入（新設 `kana` 型） | **出荷済み**（コースID: `ja-kana`。2026-07-31判断ログ#32。C=ja-0-3kの前段として配置） |

- 特化方針: **語彙のみ**（文法・会話・作文はスコープ外。例文は「語彙の文脈」としてのみ）
- メンバー系（C/D/E）に **JLPT レベルバッジ**（比国の就労日本語＝JLPT の強い動機付け。非公式リストの免責付き）
- **グロス言語の訂正（2026-07-15・判断ログ#16）**: メンバー系（C/D/E）は当初「英語」設計だったが、対象がフィリピンチームであることを踏まえ**タガログ語**へ変更。**UI言語（英語）は変更しない**——変わるのは語の意味を伝えるグロス言語だけで、画面の文言（ボタン・ラベル等）は引き続き英語。この区別により D/E は i18n（多言語UI）作業が不要（i18n が必要なのは UI 自体が日本語の A・B のみ）
- 実行フェーズの全体像・各コースの依存関係・未解決だったインフラの穴は **§7** を参照

## 2. アーキテクチャ方針: 1エンジン × 複数コース

**判断基準**: 競合10製品（Anki/jpdb/WaniKani/iKnow!/Renshuu/Clozemaster/Memrise/LingQ/Migaku/WordUp）調査で、成功例は全て次の4点に収斂。

1. **単語知識は「デッキ」でなく「ユーザー」に付く**（最重要のアーキ判断。jpdb の10状態モデルが参考: New / Learning / Known / Never-Forget / Blacklisted / Suspended / Redundant）→ 重複出題排除・カバレッジ統計・コース間移動が全部ついてくる
2. **頻度順の提示**（語彙学習 = 「有用度順の有限集合の欠損を埋める問題」）
3. **文脈（例文・クローズ）での定着**（孤立単語カードだけにしない）
4. **近代SRS**（FSRS。もはや差別化要素でなく前提）

## 3. コア機能（全コース共通エンジン）

### 3.1 復習エンジン（FSRS-6）
- ライブラリ: **ts-fsrs**（MIT・v5.4.1/2026-05・pure TS でブラウザ動作）。SM-2 比で同一保持率を約20-30%少ないレビューで達成
- ユーザー設定は **desired retention 1個だけ**（既定0.9、上級は0.85推奨表示）
- 評価 = **Again/Good の2択**既定（4択オプション）。初見「もう知ってる」→ 長期間隔
- leech 検出（lapse≥8）で自動サスペンド提案
- 約1,000レビュー後にパラメータ個人最適化（fsrs-browser WASM・2.4万レビュー≈3.5秒でクライアント側完結）

### 3.2 配置テスト（10k→30k コースの生死を決める）※コースA・E
- **Yes/No テスト＋擬似語補正**（学術定番。補正式 = (h−f)/(1−f)）
- 頻度1k帯ごとに実在語サンプリング（計60-120語・3-5分）→ **既知フロンティア（rank N 以下を一括「既知」化）**決定 → 既知1万語の学習者に既知語を挽かせない
- 第2段: フロンティア直下で4択意味マッチ10-20問→境界±1帯調整。以後 FSRS が自己修正
- **日本語注意**: 漢字複合語は字面から意味推測でき Yes/No 誤検出増（実証あり）→ 意味マッチ比重を上げる
- 0開始コース（B・C）は配置テスト不要 = レール MVP が軽い
- **実装仕様書（2026-07-15・Phase 0.5で確定）**: `docs/placement-test-spec.md` に実装可能な粒度まで落とし込み済み（採点式の数値例・アイテムバンクの形・擬似語生成方式・第2段の分岐フロー・TypeScript データモデルのスケッチ）。Phase 4着手時はこの仕様書に従う。8件の未決事項あり（バルク書き込み性能・英語擬似語の非実在チェック用ワードリスト未取得など）

### 3.3 単語状態ストア＋カバレッジ可視化
- ユーザー単位 word-state DB（**IndexedDB・ローカルファースト**）。サーバー同期は行わず、**手動 JSON エクスポート/インポート**でバックアップ・端末移行（「進捗を書き出す/読み込む」ボタン）
- 常設メーター: 「推定語彙数 12,840語」（単一数値の成長メーター）＋「あと1,000語で一般記事の理解が x%→y%」（jpdb/WordUp式 payoff）
- 「卒業（Burned）」状態で既知確定語をレビューキューから除去（3万語規模ではキュー管理が生死を分ける）

### 3.4 カードの中身（全語必須の品質バー）
- 見出し語＋グロス＋**例文2-3個**＋**発音表記**＋品詞。iKnow! Core の実証済みバーから音声を発音表記に置換
- 例文3段構え: **Tatoeba（最クリーン）→ JESC（字幕由来280万英日ペア・CC BY-SA）→ LLM生成（「AI生成」フラグ付き）**
- **音声は不採用（2026-07-13 判断）**。事前生成TTSは1万→3万語帯だけで容量試算 約0.9-1.9GB となり GitHub Pages の1GB上限を単独で超過（§6 参照）。無料の Web Speech API 版も含め音声機能そのものを撤去し、**発音表記**（後述）に一本化。データ量ゼロ・追加ホスティング不要で解決する
- **発音表記**（見出し語の下に常設表示。`VocabCard.reading` フィールドを流用）:
  - 日本語 = かな読み（JMdict由来。実装済み・追加作業不要）
  - 英語 = IPA発音記号（**CMUdict**由来。ARPABET→IPA変換テーブルを1回作れば静的データとして無料生成可。§5.1 で既に参照元として記載済み）
  - タガログ語 = アクセント位置付き表記（áso/asó 等。§4.1 で元々計画していたアクセント表示と統合）

### 3.5 Teams 連携 = 単語収穫パイプライン
**目的**: ①Teams チャットでやりとりした単語をアプリに反映 ②分からない単語をすぐ調べて学習キューへ。通知・順位表は作らない。両系統（Kohei の英語・メンバーの日本語）で同じ導線が効く。

1. **MVP: 貼り付けマイニング（統合ゼロ・当日から使える）**
   - Teams でコピー→アプリの取り込みボックスに貼付→言語自動判定→未知語抽出（既知・学習中は自動除外）→意味・例文・発音表記付きプレビュー→ワンタップでカード化 or 辞書引きだけ
   - **辞書引き（JMdict/EJDict 同梱）は完全オフラインで動く**。同梱辞書に無い新語の意味/例文のその場生成だけは外部呼び出しが要る → MVP は「同梱辞書に有る語は即カード化」、新語の LLM 生成は各自の API キーをブラウザに入れる方式で後付け（サーバーは持たない）
   - §4 の「個人マイニング」機能そのもの。URL・PDF・任意テキストにも同じ導線
2. **Phase 2: クイック辞書引き**
   - アプリ内オムニ検索（打つ/貼ると語義・頻度ランク・自分の学習状態を即表示→「学習キューへ」）。**PWA 化**でタスクバー常駐＝Teams の隣に置く
3. **Phase 3（任意）: Graph API 自動取り込み**
   - 各自の delegated 認証（Chat.Read・個人サインイン＝admin consent 不要方向）で自分のチャットを日次取得→未知語候補を朝のレビュー前に提案。**MSAL.js の PKCE でクライアントのみ完結＝バックエンド不要**なのでサーバーレスのまま追加可
   - 貼り付けで不満が出てから作る。参考: 旧 incoming webhook は 2026-05 廃止、通知が欲しくなれば Workflows webhook（無料・15分）で追加可

### 3.6 進捗・習慣化
- ダッシュボード: 推定語彙数の成長曲線・ストリーク・日次レビュー完遂
- ゲーミフィケーションは **SRS の外側**（間隔の科学を汚染しない・競合共通則）

## 4. 帯域別の学習体験（0→3k と 10k→30k は別プロダクト級に違う）

### 4.1 レール型: 0→3k（コース B タガログ語・C 日本語）
- **選択肢ゼロ**: 「開始」を押すだけ（iKnow!式）。ゲート進行（WaniKani式）で順序強制。自由度は完走率の敵
- **reading-first**（2026-07-13 音声不採用に伴い変更）: 発音表記→意味が既定の初回提示。絵/写真マッチ（具象語）・ミニマルペア聞き分け
- 受容→産出: L2→意味カードが安定（stability>7日）で 意味→L2 産出カードを自動追加
- 例文は「表示するが問わない」添え物（初期は単語単位カードが最効率という実証研究）
- **日本語コース(C)固有**: ①かなブートキャンプ（先行モジュール・ローマ字は2-4週で強制フェードアウト）②**語彙駆動の漢字導入**（jpdb式: 学ぶ語に必要な字だけ動的に。データ: KANJIDIC2＋KRADFILE＋KanjiVG筆順・全てオープン）③ふりがな表示制御
- **タガログ語コース(B)固有**: 接辞・語根表示（mag-/-um-/-in 型。同語根の派生語を「家族」で提示）、アクセント位置表記（áso/asó で意味変化。§3.4 の発音表記で対応）
- ワークロード: 新規9-10語/日 → レビュー60-80枚/日 → **1日20-30分で1年=3,000語**。最初の2週は新規5語/日ランプアップ。1日2セッション（朝レビュー/夜新語）推奨
- 区切り: 0-1k（会話の約80-85%被覆）/ 1k-3k（約95%）をマイルストーン演出

### 4.2 cloze 主軸: 3k→10k（コース D 日本語）
- 単語カードで習得 → stability 閾値超えで**文脈クローズへ自動昇格**（カード総数は増やさない）
- コロケーション導入・多義語の語義弁別・既知語率可視化リーディング（未知語タップ→デッキ追加＝マイニング入口）
- 新規10-20語/日・DR 0.85 既定・1日20-40分。7,000語=15語/日で約1.3年
- 到達目標明示: 「Band 2 完了 = 字幕なし視聴・一般書講読の入口」（8-9k語で書き言葉98%被覆）

### 4.3 較正＋マイニング型: 10k→30k（コース A 英語・E 日本語）
- **初回体験 = 配置テストで既知語一括スキップ**（WordUp の Knowledge Map 較正が直接の先行例）
- 出題形式: コロケーション cloze（"hurl accusations"型）・類義語ニュアンス4択（mitigate/alleviate/curb…）・語族展開（語根→派生形）・モノリンガル定義切替
- **個人マイニング**（この帯の新語供給の主経路）: Teams チャット・業務文書・記事の貼付→未知語抽出→ワンタップ・カード化（§3.5）。頻度リスト供給は10-15kで打ち切り、以降マイニング＋職能別パック（HR/ビジネス）へ（汎用頻度リストは15k超で信頼性崩壊）
- **現実補正**: 10k→30k の2万語は15語/日で約4-5年。20k語族≈一般書ほぼ精読可、15-20k超の限界効用は「語数」より **depth（コロケーション正答率）**へ → プログレスバーは語数＋depth の2本立て
- 新規10-15語/日＋DR 0.80-0.85 既定。忙週は新規0でレビューのみ（FSRS は中断後の再スケジュールに強い）

## 5. データパイプライン（ライセンス一次ソース検証済み・社内/個人利用前提）

### 5.1 英語（コースA: 10k→30k・グロス=日本語）※2026-07-15 Phase 0.3実測値を反映
| 層 | 採用 | 備考 |
|---|---|---|
| 頻度 | wordfreq en large（31.9万語形・rank40kまで実測確認） | レンマ化は **lemminflect で代替**（spaCyのモデルDL不要・pip installのみでオフライン動作確認済み）。524語中120語（22.9%）がレンマ化によって初めてグロスにヒットした（レンマ化必須は実測でも裏付け） |
| 既知語底面 | ~~NGSL＋NAWL＋CEFR-J A1-B2（計約8千語）~~ → **NGSL＋NAWLのみ（計3,772語・CC BY-SA 4.0）に確定（判断ログ#28）**。CEFR-Jは再配布明示許可のない独自ライセンスのため不採用 | 実効語彙数は当初想定の2万語前後よりやや広がる見込み |
| グロス | EJDict-hand（4.56万見出し・CC0。**全量取得済み**: pipeline/raw/spike-en15-30k/ejdict_src/）→LLM生成の2段（**日本語WordNet・kaikkiは不採用に確定・判断ログ#28**＝ShareAlike義務のkaikkiを避け、EJDict単体+LLMで足りる見込み） | **実測（rank15k-30k帯524語サンプル）: EJDict-hand単体で74.0%（388語・カンマ分割バグ考慮で実質約76%）がヒット。残りがLLM生成必須**。品質確認は複数AIによる相互検証（人手ネイティブレビュー無し。判断ログ#18）。フレーズ(複数語)見出し14.8%は学習カードに含める（判断ログ#28） |
| 例文 | Tatoeba（英日11.7万ペア）→ **JESC（280万・採用確定・判断ログ#28）** → LLM生成 | **実測: 英日対訳ペアで揃うのは524語中47.9%（251語）のみ、52.1%（273語）がJESC/LLM必須**だったが、**JESCカバレッジ実測（判断ログ#28）でこの273語の98.2%（268語）をJESCが埋め、LLM生成が必要な語は実質5語まで激減**することを確認。帯域が上がるほど悪化する崖（15-20k帯59.7%→25-30k帯37.8%）はJESC投入により大幅緩和見込み |
| 発音表記 | **CMUdict**（ARPABET）→ IPA 変換テーブル経由で表示 | 音声は不採用（2026-07-13）。データ量ゼロ・無料 |

### 5.2 タガログ語（コースB: 実質 0→2k・グロス=日本語/英語）※2026-07-15 Phase 0.1/0.2実測により確定（判断ログ#20）
| 層 | 採用 | 備考 |
|---|---|---|
| レンマ骨格 | **McFarland 1989 は実在・取得済み**（Wiktionary「Frequency lists/Tagalog/Top 2,000 most frequent headwords (1989)」に全2,002エントリがCC BY-SAで公開されていた。https://en.wiktionary.org/wiki/Wiktionary:Frequency_lists/Tagalog/Top_2,000_most_frequent_headwords_(1989)） | **実測確定：構造的に2,000語が原著の全量で、3,000語までは届かない**。判断ログ#19の既定方針どおり **tl-0-2k として出荷**（3kへの水増しは断念。詳細は判断ログ#20） |
| 頻度補完 | wordfreq 'fil'（top6000実測）＋OPUS OpenSubtitles v2024 自前集計（624万トークン・9.9万ユニーク語。PLANの「560万トークン」を概ね再現） | **実測: どちらも表層形（活用形）頻度で英語コードスイッチ・登場人物名の混入がrank2000超で30-38%に達し、フィルタ無しでは3k語への拡張材料に使えない**。3kまで伸ばすには接辞正規化＋固有名詞フィルタの追加実装が必要（費用対効果から見送り推奨） |
| グロス | kaikki/Wiktionary抽出（**実測: 39,025エントリ／ユニーク見出し語32,691語**＝PLAN記載の約3.3万語形と一致。取得済み: pipeline/raw/spike-tl-gloss/kaikki-tagalog.jsonl） | 日本語訳のオープンデータはゼロ→英グロスからLLM生成。**実測（Phase 0.2・50語ブラインド逆翻訳）: 94%（47/50）が完全一致**。不一致3件は語根/派生名詞の形態論的揺れとkaikki側グロスの精度問題という2パターンに整理済み→本番プロンプトでは対象タガログ見出し語もLLMへ一緒に渡す設計にすること |
| 例文 | Tatoeba TL-EN 約8,800ペア→不足は LLM生成 | |
| 発音表記 | kaikki抽出の IPA をそのまま表示＋アクセント位置マーク（áso/asó） | 音声は不採用（2026-07-13） |
| **品質確認** | **人手ネイティブレビュー無し。複数AIによる相互検証のみ**（2026-07-15判断ログ#18） | **実測（Phase 0.2）: 本環境では外部LLM APIへの接続手段が無く「別AIによる敵対的検証」は実施不可だった**。自己再点検で代替し3件の不一致を検出できたが、本番スケール投入前に別モデルでの独立検証を挟むことを推奨（未解決のまま） |

### 5.3 日本語（コースC/D/E・グロス=**タガログ語**、2026-07-15変更）※Phase 0.4実測値を反映
| 層 | 採用 | 備考 |
|---|---|---|
| 見出し語マスタ | **JMdict**（約23.5万エントリ）+ jmdict-simplified | 全土台。画面ごとに EDRDG 帰属表示が条件 |
| 読み/ふりがな | JmdictFurigana（76%解決済み）→ Sudachi フォールバック | 0-3k帯は人手全件検収 |
| レベル分け | tanos系 JLPT N5-N1（累計約8千語） | **N1/N2 CSVも取得済み**（n3-n5と同じ Bluskyo/JLPT_Vocabulary リポジトリの data/vocab/parsedData/ 配下。N1=3,475語・N2=1,844語）。C=N5+N4+N3≈3,100語がほぼそのまま骨格。D/EはN2/N1＋非JLPT語をwordfreq順で拡張 |
| 頻度 | **wordfreq ja（大規模・オープン）＋ OPUS/字幕コーパス自前集計**で頻度順を構築 | **BCCWJ・jpdb は不使用**（GitHub Pages 公開＝再配布に該当） |
| 漢字レイヤー | KANJIDIC2＋KRADFILE（部首分解）＋KanjiVG（筆順SVG） | コースC の漢字導入に必須 |
| 例文 | Tatoeba（WWWJDIC 単語→例文インデックス既製）→ JESC → LLM生成 | |
| **グロス生成（タガログ語）** | JMdict内蔵の英語グロスをLLMでタガログ語に翻訳 | **JMdictはタガログ語グロスを持たない**ので必須の追加ステップ。コースC は `build_ja_0_3k.py` を実行せず出力JSONを直接手翻訳した経緯があり（判断ログ#16参照）、D/Eからはこのステップをパイプラインへ正式に組み込む（`glossLanguage:"English"` のまま残る同スクリプトのバグ修正を兼ねる） |
| 発音表記 | JMdict の読み（かな）をそのまま表示（**実装済み**） | 音声は不採用（2026-07-13）。かな読みで発音は一意に伝わるため追加データ不要 |
| **品質確認** | **人手ネイティブレビュー無し。複数AIによる相互検証のみ**（2026-07-15判断ログ#18） | 生成AIと別のAIが敵対的に検証するパターンを想定。品質リスクは§9参照 |

**Phase 0.4実測結果（帯域ごとの実現可能性が大きく異なることが判明）:**
- **コースD（3k→10k・Phase 3）: フル充足確認・そのまま着手可（判断ログ#21）**。N2/N1×JMdictのマッチ率99.1%（ja-0-3kの98.4%と同等以上）。既存 `ja-0-3k` との重複除外後、N2/N1だけで必要7,000語の73%が埋まり、残りをwordfreq順のJMdict拡張で補って7,000/7,000語フル充足・目視サンプルも良質。既存 `match_jmdict()` 等のロジックはそのまま流用可。**唯一の実装時注意点**: N2ソースCSVに単一カナの断片行が7件混入（「た」「い」等がJMdictの無関係語に誤マッチする）——`kanji==reading かつ長さ1` の行を除外する1行フィルタを追加すること。
- **コースE（10k→30k・Phase 4）: 実質「ja-10k-23k」相当に縮小確定（判断ログ#22）**。安全な頻度シグナル（JMdict `common` フラグでゲート）だけでは12,935/20,000語（65%）で頭打ち——7,065語（26%）が不足する。フィルタを外して素の wordfreq 全展開を使えば数の上では20,000/20,000埋まるが、同音異義語への頻度誤付与（例: 稀語「画架」が読み『がか』経由で常用語「画家」の頻度を借用してしまう）・助詞助動詞などの文法語混入を実測で確認済み（無フィルタ時の混入率3k-10k帯1.4%・10k-30k帯2.7%、フィルタ後は両帯とも0.2%台まで低下）のため、**品質妥協・追加コーパス投入は不採用とし範囲縮小を選択**（タガログ語コースBのtl-0-2k判断=判断ログ#19・#20と同じ「無理な水増しをしない」方針）。

### 5.4 ライセンス衛生（**public 配信前提＝最初から再配布クリーン**）
- **GitHub Pages 公開＝インターネットへの再配布**。研究/教育限定・再配布不可のデータは同梱しない。データ層（CC BY-SA / CC0 等の再配布可）と独自コンテンツを分離したパッケージ構造は維持（将来の外販でも同じ層をそのまま使える）
- アプリ内クレジット画面（EDRDG・wordfreq/SUBTLEX・tanos・CEFR-J 指定引用・日本語WordNet）
- Tatoeba は文単位の帰属メタデータをスキーマ保持。CC BY-SA 継承の表示義務を各画面で満たす
- **シークレットは public リポジトリに置かない**。新語マイニングの LLM キーは各自がブラウザに入れる方式でコミットしない
- 検証の結果**使わない**もの: **BCCWJ（研究/教育限定＝公開再配布不可）・jpdb（再配布不可）**・Oxford 3000/5000（OUP著作権）・Forvo（再配布不可）・JapanesePod101系音声（無許諾）・共有Ankiデッキ（ライセンス不明/有償。品質ベンチマークとしてのみ参照）

## 6. 技術スタック（サーバーレス・100% クライアントサイド）

- **フロント**: **Vite＋React＋TypeScript の静的 SPA**（バックエンド無し。Next.js のサーバー機能は使わないため純粋な静的ビルドの Vite を採用）。**PWA 化**して「Teams の隣に常駐する辞書＋学習アプリ」に
- **配布/ホスティング**: **個人 GitHub の public リポジトリ → GitHub Pages（無料）**。メンバーは URL を開くだけ、PWA でインストールも可。アプリサーバー・DB は一切なし。※GitHub Pages の公開サイトは常に「URL を知れば誰でも閲覧可」（閲覧制限は Enterprise 専用）＝§5.4 の再配布クリーンが前提
- **進捗データ**: IndexedDB（Dexie）**ローカルファースト＝各人の進捗はその端末に**。アカウント無し。クラウド同期はせず、**手動 JSON エクスポート/インポート**でバックアップ・端末移行（データ消失対策）
- **語彙データ**: 静的 JSON パック（コース別・1k帯別に分割・遅延読込）。**音声は不採用**（2026-07-13）＝発音は `reading` フィールドの発音表記（かな/IPA/アクセント付き表記）で伝える
- **容量試算（音声不採用の根拠）**: 1万→3万語帯（2万語）に事前生成TTSを載せた場合、48kbps mono 換算で1語あたり約70KB（見出し語+例文2-3個）×2万語 ≈ **1.33GB** となり GitHub Pages の1GB上限を単独で超過（低音質32kbpsでも約0.94GBでほぼ余地なし）。発音表記に切り替えたことでこの制約自体が解消し、別ホスティングの検討は不要になった
- **SRS**: ts-fsrs（MIT）＋ fsrs-browser（WASM）。すべてブラウザ内で完結（サーバー不要）
- **データパイプライン**: Python ローカルバッチ（wordfreq / Sudachi / spaCy / 頻度集計 / CMUdict変換等）を**手元で実行**し、成果物（静的 JSON）だけをリポジトリに置く。実行時サーバーは無い
- **Mock-First**: UIトラック（モックデータ50語で学習ループUIを磨く）とデータspikeトラック（パイプライン検証）を並走→合流

## 7. フェーズ計画（v1.5・確定。2026-07-15 Kohei 判断で並行案から直列＋明示インフラ回に変更）

**v1.4までの並行案からの訂正**: 「コースB（タガログ語）は既存レールエンジンのデータ差し替えだけ」という前提は誤りだった。実装調査の結果、コース切替UI・コースごとの引用元表示（Credits）・カバレッジ計算・多言語UI(i18n) が**すべてコースC専用にべた書き**されており、`CourseType`（rail/cloze/calibrate-mine）という型は定義されているだけで**コード上どこからも読まれていない**（＝cloze・較正は現状ゼロから作る新規エンジン）ことが判明。これを踏まえ、共通基盤を独立フェーズ（**Phase 1.5**）として切り出し、残り4コースは総当たりの並行でなく**1コースにつき新しい難所を1つだけ切り分ける直列**で進める。

```
Phase 0  データ・仕様の下調べ（実測スパイク・使い捨て調査。実装はしない）【完了・2026-07-15】
  ├─ 0.1 タガログ語データ ✓完了: McFarland実在確認・2,000語が構造的上限と実測確定→tl-0-2k（判断ログ#20）
  ├─ 0.2 タガログ語グロス生成 ✓完了: kaikki英グロス→タガログ語訳（50語ブラインド逆翻訳94%一致）
  ├─ 0.3 英語15k-30k帯 ✓完了: グロス74%・例文(英日対訳)47.9%を実測。JESC補完分は次スパイクで要追加測定
  ├─ 0.4 日本語3k-30k骨格 ✓完了: コースD(3k→10k)はフル充足確認（判断ログ#21）。コースE(10k→30k)は65%止まり→Kohei判断待ち
  └─ 0.5 配置テストの仕様確定 ✓完了: docs/placement-test-spec.md（300行）に実装可能な粒度で確定

Phase 1.5  共通基盤（新設・2コース目を出す前に一度だけ作る）
  ├─ 1.5.1 コース切替UI（App.tsx の COURSE_ID 定数を廃止し実行時選択に）
  ├─ 1.5.2 コース・レジストリ（コース一覧を1箇所で管理。コース追加をコード変更でなくデータ追加にする）
  ├─ 1.5.3 コースごとの引用元表示（Credits.tsx をコースの出典メタデータ読み込みに変更）← ライセンス表記の正確性に関わる必須修正
  ├─ 1.5.4 カバレッジ計算の汎用化（coverage.ts の「3000語で頭打ち95%」を廃し、10k-30k帯は語数＋深度の2軸表示に）
  ├─ 1.5.5 i18n基盤（uiLanguage で切り替わる文言テーブル。対象はUIが日本語のコースA・Bのみ——C/D/EはUI英語のまま変更不要）
  ├─ 1.5.6 コーチメッセージの日本語版プール（coachMessages.ts に JA_POOLS を追加。対象はA・Bのみ）
  ├─ 1.5.7 コーチ文スキーマの汎用化（coachSentences.ts の jp/en 固定フィールド名を言語ペア非依存に）
  └─ 1.5.8 PWAキャッシュ戦略の見直し（vite.config.ts が全コースのJSONを全訪問者に配ってしまう問題。使うコースだけ配信するruntimeCachingへ）

Phase 2  コース B（タガログ語 0→2k・Kohei向け・UI日本語）※既存レールエンジンをそのまま流用 【完了・2026-07-15・判断ログ#23】
  ├─ ✓ JA_POOLS動的テンプレのギャップは解消済み（coach.tsにisJa(c)判定を追加）
  ├─ 2.1 pipeline/build_tl_0_3k.py 新規作成（McFarland×wordfreq'fil'×OPUS集計。pipeline/emit.py は流用可）
  ├─ 2.2 グロス生成（kaikki英グロス→LLM日本語訳。複数AI相互検証のみ・人手レビュー無し＝判断ログ#18）
  ├─ 2.3 例文（Tatoeba TL-EN 約8,800ペア＋LLM補完）
  ├─ 2.4 タガログ語固有表示（接辞・語根表示、アクセント位置表記。romaji.tsは変更不要＝既に非かな入力をnull返却する設計）
  ├─ 2.5 categories.json・coach-sentences.json（生成スクリプト無し。コースCと同じ「AI生成→AI相互検証」の手順を踏む）
  └─ 2.6 レジストリ登録・引用元登録・Playwrightで実画面確認

Phase 3  コース D（日本語 3k→10k・メンバー向け・UI英語・グロスはタガログ語）※cloze エンジンを新規開発
  ├─ 3.1 データ拡張（build_ja_0_3k.py の JMdict/頻度/例文ロジックを再利用し3k→10k帯へ。ja-0-3k収録語は除外。
  │      グロスのタガログ語訳ステップをパイプラインに正式組込——コースCは手翻訳だった経緯を解消。判断ログ#16）
  ├─ 3.2 clozeセッションモデルの新規実装（useStudyBoard.ts は現状レール専用構造。course.type分岐を新設）
  ├─ 3.3 clozeカードUI（安定度が閾値を超えたら文脈クローズへ自動昇格。カード枚数は増やさない）
  ├─ 3.4 PLAN §4.2フル仕様（コロケーション導入・多義語弁別・未知語タップ→マイニング導線）を段階分けせず一括実装
  │      （判断ログ#17：MVP先行でなくフルスコープを最初から選択したため、当初想定より開発規模が大きい）
  ├─ 3.5 categories.json・coach-sentences.json（jp/enスキーマ流用。AI生成→AI相互検証）
  └─ 3.6 レジストリ登録・引用元登録・Playwright確認

Phase 4  コース E（日本語 10k→30k・メンバー向け・UI英語・グロスはタガログ語）※較正＋マイニングエンジンを新規開発
  ├─ 4.1 データ拡張（10k→30k帯。Tatoebaカバレッジ薄→LLM比重増）【完了・2026-07-18・実質10k-23k=12,735語で出荷。JESC不使用＝LLM生成例文で補完】
  ├─ 4.2 配置テストエンジン ❌**不採用＝削除**（判断ログ#26。FSRS＋「知ってる」ボタンで既知語スキップは足りる／過剰な複雑さ）
  ├─ 4.3 PLAN §4.3フル仕様（コロケーションcloze＝Phase3のカードを再利用・類義語ニュアンス4択・語族展開・モノリンガル定義切替）❌**不採用＝作らない**（判断ログ#27。#26と同じ「深度強化はコア機能でない・無理をしない」方針）
  ├─ 4.4 語数＋深度の2軸プログレス表示（1.5.4の汎用化を利用）✓**追加着手不要＝実質完了**（判断ログ#27。1.5.4で`courseProgress()`が既に配線済み・depth=null時は語数のみ表示する安全設計で稼働中。4.3不採用によりdepthを埋める作業も発生しない）
  └─ 4.5 データ・レジストリ・引用元・Playwright確認【実質完了・2026-07-18＝courseRegistry登録済み・cloze実機確認済み・引用元meta.json記載済み・デプロイ済み】

  ※配置削除後、コースEは「cloze主軸コース（Phase3エンジン再利用）＋将来のマイニング（Phase6）」として既に機能・出荷済み。4.3不採用・4.4完了によりPhase 4は**完了**（判断ログ#27・2026-07-18）。次はPhase 5（コースA英語）。

Phase 5  コース A（英語 実質4k→24.5k・Kohei向け・UI日本語・グロス日本語）※較正エンジンはPhase4流用、英語データは完全新規【判断ログ#28-#30】
  ├─ 5.1 英語データパイプライン新規構築【完了】pipeline/gen_en_wordfreq_ranked.py（wordfreq large×レンマ化・NGSL+NAWL既知底面カット。CEFR-Jは不採用#28）
  ├─ 5.2 グロス生成【完了】pipeline/ejdict.py（EJDict-hand専用パーサ）。日本語WordNet・kaikkiは不採用（#28）
  ├─ 5.3 例文・発音表記【完了】pipeline/build_en_10_30k.py（Tatoeba転置→JESC→LLM／pipeline/arpabet_to_ipa.py自前実装でCMUdict→IPA）
  ├─ 5.4 LLMコンテンツバッチ生成【完了・2026-07-25】400バッチ・30,000語・エラー0件（判断ログ#30）。isValidVocabulary判定で意味的ノイズ除外＋日本語グロス確定＋カテゴリ分類＋クローズ例文
  └─ 5.5 データ・レジストリ・引用元・出荷確定【データ/レジストリ/引用元は完了・2026-07-25】pipeline/emit_en_10_30k.py実行→public/data/courses/en-10-30k/（20,725語・21バンド）・courseRegistry.ts登録・typecheck/build通過。**残作業**: ①Playwright目視確認（本セッションは接続不可・要次回実施）②coach-sentences.json（未生成・404時空配列フォールバックのため出荷ブロッカーではない）③push/デプロイの実施

Phase 6  運用改善（Phase 2-5 出荷後に再計画）
  ├─ 手動 JSON バックアップ UI 改善
  ├─ Teams 貼り付けマイニング MVP（実装前に autonomous-agent-safety スキルを読む——外部LLMへのデータ送信を伴うため）
  ├─ Graph API 自動チャット取り込み（貼り付けで不満が出てから）
  └─ 職能別パック（HR/ビジネス）
```

**着手順の根拠**（判断ログ#14）: 新規エンジン開発コスト（安→高: レール流用のPhase2 ＜ clozeのPhase3 ＜ 較正のPhase4・5）、データ調達リスク（安→高: 実績あるJAパイプラインのPhase3・4 ＜ ゼロから作るPhase2・5）、i18nコスト（Cloze/較正のPhase3・4はUI英語のままなので0、Phase2・5は日本語UI化が必須）という3軸が独立して効くため、**Phase2→3→4→5 の順で1コースにつき新しい難所を1つだけ切り分ける**。cloze→較正という自然なエンジン依存（較正のコロケーションclozeはclozeカードUIを再利用）も直列順に合致する。

**フェーズ番号とコース記号（A〜E）は対応しない**——コース記号は §1 の表（学習内容の分類）、フェーズ番号は着手順（実行の順序）で、意図的に別軸。例: Phase 2 はコース B の実装、Phase 5 はコース A の実装。

## 8. 判断ログ

| # | 論点 | 結論 | 日付 |
|---|---|---|---|
| 1 | 用途方針 | 社内・個人限定（販売しない前提） | 2026-07-13 |
| 2 | 利用者モデル | 複数人・各自が個人用（チーム共有機能なし） | 2026-07-13 |
| 3 | MVP 順序 | 並行（Kohei系＋メンバー系を共通コアで並走） | 2026-07-13 |
| 4 | Teams 連携 | チャット単語マイニング＋辞書引き。通知/順位表は作らない | 2026-07-13 |
| 5 | 配置場所 | 01_Accenture/Vocab_Trainer | 2026-07-13 |
| 6 | アカウント/同期 | アカウント無し・ローカルファースト。クラウド同期はせず手動 JSON バックアップ（**Supabase 不採用**）※**#33 で一部変更**——学習の一次データが端末内 IndexedDB である点は維持しつつ、管理者向けに「コース別の集計値だけ」をサーバへ片方向送信するようになった | 2026-07-13 |
| 7 | バックエンド方針 | **無し＝サーバーレス**（100% クライアントサイド） | 2026-07-13 |
| 8 | 配布/ホスティング | 個人 GitHub の **public リポジトリ → GitHub Pages（無料）**。公開サイトは URL を知れば誰でも閲覧可（制限は Enterprise 専用） | 2026-07-13 |
| 9 | フロントスタック | **Next.js → Vite 静的 SPA**（サーバー機能不要のため） | 2026-07-13 |
| 10 | 日本語頻度データ | **BCCWJ・jpdb 不使用** → wordfreq ja 等のオープン頻度（public 再配布のため） | 2026-07-13 |
| 11 | COCA | $295=買わない見込み | 推奨案既定採用 |
| 12 | コースC 実データ | **完了**：JLPT N5-N3（Bluskyo/JLPT_Vocabulary, tanos.co.uk CC BY）× JMdict（EDRDG）× wordfreq ja × Tatoeba(CC BY 2.0 FR) で3,075語を実生成。JMdictマッチ率98.4%・例文カバレッジ99%。BCCWJ/jpdb不使用の方針どおり全て再配布可能データ。GitHub Pagesで公開済み | 2026-07-13 |
| 13 | **音声機能** | **不採用**（Web Speech API 版含め全面撤去）。1万→3万語帯で事前生成TTSが単独で1GB上限を超過する試算（§6）を受けての判断。代わりに**発音表記**（日本語=かな／英語=IPA・CMUdict／タガログ語=アクセント付き表記）を `reading` フィールドで統一表示 | 2026-07-13 |
| 14 | 残り4コースの着手順 | **Phase2(コースB)→Phase3(コースD)→Phase4(コースE)→Phase5(コースA)**。新規エンジン開発コスト・データ調達リスク・i18nコストの3軸が独立して効くため、1コースにつき新しい難所を1つだけ切り分ける直列進行を採用（並行案は不採用） | 2026-07-15 |
| 15 | 共通基盤（Phase 1.5）の作り方 | **2コース目を出す前に一度だけまとめて作る**（コースごとに最小限だけ直す案は不採用）。コース切替UI・引用元表示・カバレッジ汎用化・i18nは後回しにすると手戻りリスクが大きいと判断 | 2026-07-15 |
| 16 | メンバー系（コースC/D/E）のグロス言語 | **タガログ語を正とする**（英語ではない）。ja-0-3k のグロスは2026-07-15にタガログ語へ手翻訳済み（フィリピンチーム向け判断）。パイプラインスクリプト（`build_ja_0_3k.py` は現状 `glossLanguage:"English"` のまま）とPLAN.mdの記載が実データと食い違っていたため、タガログ語を正として揃える。**UI言語（英語）は変更しない**——変わるのはグロスのみ | 2026-07-15 |
| 17 | cloze・較正エンジンの実装範囲 | **PLAN §4.2/§4.3 のフル仕様を最初から実装**（MVPを経て段階拡張する案は不採用）。開発規模は当初見積もりより大きくなる（Phase 3・4 は XLサイズ） | 2026-07-15 |
| 18 | コンテンツ生成の品質確認体制 | **人手ネイティブレビュー無し。複数AIによる相互検証のみ**（フィリピンチームのネイティブへの依頼、Kohei自身のサンプリング確認は不採用）。タガログ語グロス・英語グロス・例文・カテゴリ分類・コーチ文すべてに適用。品質リスクは§9参照 | 2026-07-15 |
| 19 | コースB（タガログ語）の語数上限 | McFarland等のオープンデータが3,000語に届かない場合、**無理な水増しをせず届いた語数（例: tl-0-2k）で出す**。Phase 0.1 の実測結果を待って確定 | 2026-07-15 |
| 20 | Phase 0.1/0.2実測結果の適用 | McFarland 1989はWiktionary経由で実在・取得済み（構造的に2,000語が上限で3,000語には届かない）と実測確定。**判断ログ#19の既定方針をそのまま適用し、コースBは tl-0-2k として出荷**。新たな判断は不要（#19の範囲内）。タガログ語グロス生成もPhase 0.2で50語ブラインド逆翻訳94%一致を確認、実用レベルと判断 | 2026-07-15 |
| 21 | コースD（3k→10k）のデータ実現可能性 | Phase 0.4実測でN2/N1×JMdictマッチ率99.1%・7,000語フル充足を確認。**そのまま既存パイプライン流用でPhase 3着手可**（Kohei判断不要の技術確認） | 2026-07-15 |
| 22 | コースE（10k→30k）の語数上限 | 安全な頻度シグナル（JMdict commonフラグ）だけでは12,935/20,000語（65%）止まり。**範囲を縮小し実質「ja-10k-23k」相当で出荷**（品質妥協・追加コーパス投入は不採用）。タガログ語コースBのtl-0-2k判断（#19・#20）と同じ「無理な水増しをしない」方針を踏襲。コース名・表示は現行のまま「10k→30k」を残すか改称するかはPhase 4実装時に決定 | 2026-07-15 |
| 23 | コースB出荷・敵対的検証で発見した実バグ | **完了**：`tl-0-2k` を2,000語で出荷。McFarland 1989はWiktionary経由で実在確認済み（判断ログ#20）、kaikki/Tatoebaと統合。敵対的検証（判断ログ#18の実施）で**コーチ文170件中93件（当初推定84件より多い）で `words[]`（既習語ゲート）宣言漏れという実バグを検出・修正**——これはグロス自体の誤りではなく、「既習語だけで組んだ文だけを見せる」というアンロック機能の保証が壊れていた実装バグ。グロス側は100語サンプルのブラインド逆翻訳検証で一致率97%（軽微な指摘3件のみ、再生成不要と判定）。**検証プロセスが機能した実例**として記録（人手ネイティブレビュー無し方針＝判断ログ#18のリスクを、機械的な独立検証で実際に相殺できた） | 2026-07-15 |
| 24 | コースD出荷＋横展開バグ | **完了**：Phase 3 で cloze エンジン＋コースD（ja-3-10k・7,000語）を出荷。表示名確定・cloze実機確認済み。 | 2026-07-16 |
| 25 | コースE出荷（表示名＝改称）＋Tatoeba品詞不一致バグの横展開修正 | **完了**：Phase 4.1 で コースE（ja-10-30k・12,735語）を出荷。**表示名は「Japanese 10,000 → 23,000」に改称**（#22の宿題を解消。実データが実質10k-23kのため正直に。内部IDは `ja-10-30k` 据え置き）。例文は Tatoeba＋LLM生成（`aiGenerated:true`フラグ・#18のAI相互検証）。**敵対的検証で「名詞見出し語カードに動詞活用形の例文が誤爆する品詞不一致バグ」を発見**——Tatoebaマッチングが品詞（レンマ）一致を見ていない不具合で、**既出荷のコースC(34件)・D(108件)にも実在**と判明。E(96件)と合わせ計235カードから不良例文424件を除去し3コースまとめて再デプロイ。AI生成例文では0件（緩和策として有効）。根本対策（pipelineへのPOS一致チェック追加）は将来TODO。 | 2026-07-18 |
| 26 | **配置テスト（プレースメント/レベル振り分け）の要否** | **不採用＝作らない**（Phase 4.2 を削除）。理由：①コースEの語は10k-23k帯からコースC/D既習語を除いた「本当にまだ知らない語」で、スキップすべき既知語の山が構造的に存在しない ②C→D→Eと進む学習者の到達点はアプリが既に把握しており、テストせずとも「1万語地点」は自明 ③FSRS＋「知ってる」ボタンが既に既知語スキップを自然に担い、仕様書自身が「配置＝I know 1回押下と等価・誤判定はFSRSが自己修正する安全弁」と記載＝必須でなく便利機能。過剰な複雑さ（状態機械＋擬似語ビルド時生成＋バルク書込）に対し費用対効果が悪い。「無理をしない」方針（#19・#22）と一致。副次効果でPhase 5コースAの英語擬似語ワードリスト未取得ブロッカーも消滅。`docs/placement-test-spec.md` は不使用として棚上げ（削除はしない＝将来外部上級学習者向けに再検討の余地）。将来コールドスタート上級者対応が要れば統計テストでなく「だいたい何語知ってる？」の軽量スライダー（rank N以下を一括既知化）で足りる | 2026-07-18 |
| 27 | **Phase 4.3（深度機能）・4.4（2軸プログレス）の要否** | **4.3は不採用＝作らない。4.4は追加着手不要（実質完了）**。理由：①4.3（類義語ニュアンス4択・語族展開・モノリンガル定義切替・コロケーション正答率スコアリング）は類義語・語族データがゼロから必要でPhase3級の新規開発規模になる一方、コースEはPhase3エンジン再利用のcloze主軸コースとして既に出荷済み・学習ループとして完結している。判断ログ#26（配置テスト不採用）と同じ「深度強化はコア機能でない・無理をしない」方針を踏襲。②4.4（語数＋深度2軸プログレス）はPhase 1.5.4で先行配線済みと判明——`coverage.ts` の `courseProgress()` が calibrate-mine型（コースE）で `SizeDepthProgress`（words＋depth）を返し、`CourseScreen.tsx`・`StatsPanel.tsx` に配線済み。depthは実データ源（4.3のコロケーション採点）が無いため常にnullだが、null時は語数のみを表示する安全設計で既に動作している。4.3を採らない以上、depthを埋める作業も発生しない＝4.4は追加実装ゼロで現状のまま。これによりPhase 4は実質完了。次はPhase 5（コースA・英語）に進む | 2026-07-18 |
| 28 | **Phase 5（コースA英語）データソース調査結果への対応** | 調査で判明した最重要ギャップ：**英語見出し語プール自体を頻度リストから列挙するコードがリポジトリに存在しない**（日本語コースはJMdictという権威的見出し語集合を頻度で並べ替えるだけだったため、英語では逆に「頻度リストから見出し語プールを新規に作る」処理が要る＝Phase 5.1最優先の新規実装）。ライセンス判断4点は以下で確定：①**CEFR-J（既知語底面の第3ソース）は不採用**——「引用すれば無償利用可・商用要相談」の独自条項で再配布明示許可なし。判断ログ§5.4（BCCWJ/jpdb/Oxford等の不採用基準）と一貫。既知語底面はNGSL+NAWL（CC BY-SA 4.0・計3,772語）のみで進める。②**kaikki（グロス第3層・英語Wiktionary抽出）は不採用**——CC BY-SA+GFDLでShareAlike義務を伴う。EJDict-hand単体で実測76%カバー＋LLM生成で埋まる見込みのため無くても成立。③**EJDict見出しの14.8%を占めるフレーズ（複数語）見出しは含める**（Koheiの明示判断。他コースの「1語=1カード」前提とは異なる設計になるが、コロケーション・熟語は上級英語学習の価値が高いため採用）。④**高rank帯（25k-30k、例文カバレッジ実測37.8%まで低下）は実測で届いた語数で確定**——コースE(#22)と同じ「無理をしない」方針。20k到達を目標に固定せず、範囲縮小・表示名改称も許容。EJDict-hand本体（CC0・全45,631件）・CMUdict（BSD類似）・Tatoeba既存4ファイル（転置流用）は問題なく採用確定。**⑤JESC（CC BY-SA・例文第2源）は採用確定**——Phase5.0必須ゲートの実測結果（`pipeline/raw/spike-en15-30k/jesc_coverage.py`）: Tatoebaが埋められなかった273語（no-EN-sentence 14語+no-JA-link 259語）のうち**98.2%（268語）をJESCがカバー**、JESCでも未カバーは5語（interpretive・qur'an・plenary・netball・borderlands）のみ。LLM生成依存率が52.1%→実質1.8%相当まで劇的に下がるため、CC BY-SAのShareAlike義務（Tatoebaで既に対応済みの引用表示パターンを踏襲すれば足りる）を受け入れて採用。5.1a実装着手済み: `pipeline/gen_en_wordfreq_ranked.py`で頻度リストからの見出し語プール列挙（候補69,913レンマ）を実装・実行確認済み（実装中にlemminflectのPOS盲目レンマ化バグとNGSL/NAWL既知語ロードの列漏れバグの2件を発見・修正） | 2026-07-18 |
| 29 | **コースA語彙への俗語・卑語（shit/fuck等）の扱い** | **含める**（Kohei判断）。頻度リスト上位（rank6,8等）に実在の高頻度語として出現。実際の英語使用頻度を反映する実用重視の設計として採用。他コース（C/D/E）に俗語カードが存在しないこととは非対称になるが、コースAは較正＋マイニング型（実データ準拠）であり英語ネイティブの実使用に即した収録という位置づけ | 2026-07-18 |
| 30 | **コースA最終出荷スコープ・命名の確定** | 5.4のLLMコンテンツバッチ生成（400バッチ・30,000語）が全件完了（エラー0件）。有効判定率69.3%（20,784/29,990語）。ここで「10k→30k」という命名の前提が崩れていることが判明——CEFR-J不採用（#28）で既知語底面がNGSL+NAWLの3,772語のみとなり、候補プールの実頻度順位は89位〜49,268位に広がっていた（当初想定の「10,000位〜30,000位」と不一致）。実データ確認の結果、実頻度10,000位未満の語は全体の13%程度（多くはNGSLが数字を収録していないための漏れ）で、87%は文字通り1万位以降の中級〜上級語彙と判明。**Kohei判断：数詞・代名詞等の明らかな基礎語のみ除外し残りは全て収録**。精査の結果、機械的に除外すべきは pos=数詞（two/three/…等59語。NGSL漏れの明確な基礎語）のみと確定——pos=代名詞/前置詞/接続詞/助動詞に分類された語（thou/thy/whomever/notwithstanding/albeit/shalt等）は全て古語・文語・略式表現であり「基礎語」ではないため除外せず。最終収録20,725語。**表示名はsibling courses（ja-3-10k/ja-10-30k）と同じ「累積到達語彙数」方式に統一**——「English 3,772 → 24,497」（既知語底面3,772語からコースA収録後の累積24,497語）。内部courseId=`en-10-30k`は維持（types.tsに既存宣言済み）。pipeline/emit_en_10_30k.pyでstage2実装・実行、public/data/courses/en-10-30k/へ出力（21バンドファイル）、courseRegistry.tsに登録。typecheck・build共に通過。Playwright MCPは本セッションで接続不可（ECONNREFUSED ::1:9222）のため目視確認は未実施——次回セッションで要実施。coach-sentences.jsonは未生成（404時は空配列フォールバックする設計のため出荷ブロッカーではない・後追い対応） | 2026-07-25 |
| 31 | **コースA表示名の開始番号を10,000に変更** | #30で確定した「English 3,772 → 24,497」（技術的に正確な既知語底面3,772語基準）から、**Kohei判断で開始番号を「10,000」に変更**——Phase 5当初の企画名「英語10k→30k」の座りの良さを優先。実際の収録語数（20,725語）はそのまま保つため終了番号も連動してスライドし、**「English 10,000 → 30,725」に確定**。frequencyRankの起点も10,001からに変更（word一覧の#表示とタイトルの整合性を保つため）。pipeline/emit_en_10_30k.pyは表示用の`DISPLAY_BASELINE`(10,000)と引用元citation用の正確な`NGSL_NAWL_COUNT`(3,772)を分離——meta.jsonのNGSL/NAWL出典note には正確な3,772語を残しつつ「タイトルは丸めた開始番号を表示している」旨を明記し、ライセンス表示の正確性は保持。band file名・categories.jsonキーも新frequencyRankで再生成 | 2026-07-30 |
| 32 | **かな導入コース（ja-kana）の新規追加** | Kohei依頼：「ひらがなカタカナのバージョン」＝ja-0-3kより手前に置く、見出しがかな1文字（1音）のコース。スコープは「かなが読める」を最終ゴールとする完全セット（清音46＋濁音/半濁音25＋拗音33を hiragana/katakana 各104字＝計208枚。促音「っ」・長音記号「ー」は単独の音価を持たないため対象外）——清音のみだと が/ば/きゃ 等の頻出表記が読めないまま次コースに進んでしまい導入コースとして不十分なため。CourseType を `'rail'|'cloze'|'calibrate-mine'` の3値から `'kana'` を追加した4値に拡張——`rail`/`calibrate-mine` 流用だと `cloze.ts` の自動クローズ昇格対象に入ってしまう／`coverage.ts` の会話被覆率%文言が208枚規模には意味を成さないため。`kana` は `calibrate-mine` と同じ「語数のみ」表示（depth常にnull）を流用しクローズ昇格対象からは自然に外れる。`CourseScreen.tsx` の `MilestoneChip`/`MilestoneOverlay`（`coverageAt()` 直呼びの既存の技術的負債）は本コースだけ表示をガードして対応（根本設計は今回のスコープ外）。かな→ローマ字→カタカナの対応表はJMdict等の外部データに依存せず `pipeline/build_ja_kana.py` に静的に埋め込み、値は app 本体が表示に使う wanakana（既存依存）の toRomaji/toKatakana で1件ずつ検算（自動照合スクリプトで104件全一致を確認）してから手打ちの誤記リスクを消した。例文/例語は v1では付けない（かな文字は文脈でなく認識の反復が本質という判断／新規のJMdict・LLM連携は過剰投資）。`AVAILABLE_COURSES` の先頭に登録し新規ユーザーの既定コースに。typecheck・build・Playwright目視確認（コース切替・カード表示/裏返し/採点・進捗が%でなく語数表示になること・All words一覧）済み | 2026-07-31 |
| 33 | **管理者画面の追加（判断ログ#6を一部変更）** | Kohei依頼：利用者の登録・削除と簡単な進捗確認を管理者が画面から行いたい。アプリはアカウント無し・進捗は端末内IndexedDBのみだったため、①「誰がログインできるか」②「各自の進捗を集約する場所」の2点をどこに置くかが論点。**Kohei判断：Cloudflare D1 に自動同期する／「登録・削除」はログイン許可そのものを操作する**（代替案の「サーバー無し・JSON持ち寄り」「Supabase」は不採用——Access が既に本人確認を済ませているのに Supabase Auth を足すと認証が二重になり構成が重くなるため）。設計：認証は自作せず Cloudflare Access の JWT（Cf-Access-Jwt-Assertion）を Worker で検証するだけ。ログイン可否の真実は **Zero Trust の Emails 型リスト1か所**で、管理画面の登録／削除はそのリストの append/remove そのもの（D1の名簿だけ書き換える設計にすると「名簿にいるのに入れない」ズレが必ず起きるため）。D1 に置くのは**コース別の集計値のみ**（語ごとの学習データ・回答履歴は送らない）で、学習の一次データは引き続き端末内IndexedDB＝D1が消えてもオフラインでも学習は壊れない関係を維持。判断ログ#6の「クラウド同期なし」はこの範囲で変更、「Supabase不採用」は維持。ルートは main.tsx の既存ハッシュルーター（#design/#tones/#growth）に `#admin` を追加（パスルートにすると SPA フォールバック設定が必要になり、存在しないJSONの404フォールバックを壊すため）。security-reviewer のレビューで CSRF（Access の Cookie は既定 SameSite=None のため JWT 検証だけでは防げない）等 HIGH1/MEDIUM3 を検出し全て解消。**リポジトリが public なので設定値（メール・アカウントID・リストID・APIトークン）は wrangler.jsonc の vars に置かず全て Worker Secret**。詳細設計とセットアップ手順＝ docs/admin-console.md | 2026-07-31 |

| 34 | **コースA（en-10-30k）の「簡単すぎる語」を多指標フィルタで一括除去（20,532→12,460語）** | Kohei報告：「明らかに簡単な単語が混ざっている。収録語数が減ってもよいので除きたい」。**根本原因は既知語底面がNGSL+NAWLの3,772語しかないこと**——#31で表示開始番号を10,000に丸めたが、実際の足切りは3,772語のままなので、3,772〜10,000相当の約6,000語が構造的に混入していた。過去の193語手動除去は zipf>=4.3 という閾値ゲートで recall が頭打ちになっており（zipf<4.3 の簡単語は一度も見られていない）症状対処に留まっていた。**単一の統計軸をやめ、直交する指標の和集合＋グレーバンドのAIレビューに置き換えた**。実測した機械フィルタ：F1 Tatoebaでの非文頭大文字率>=0.85（固有名詞1,419語）／F2 AoA<=8.0歳（幼少期習得の具体語2,571語）／F4 既知語からの-ly/-ness/-ful/-less派生（483語）／F5 AoA・prevalence・EJDictのどれにも載らない語（略語・スラング2,469語）／F6 pos=固有名詞（LLMが既に付けていたが#30で未活用だったタグ。274語）。**AoAが決め手**——carrot/spoon/puppy のような「幼児期に覚えるが大人のコーパスでは低頻度」の語は zipf が原理的に分離できず、習得年齢データだけが直接測れる。残ったグレーバンド（容疑スコア>=3の3,960語）は Sonnet 22バッチでAIレビューし1,476語を追加除去（判定既定を「迷ったら削除」に反転。#32のprecision優先から recall 優先へ）。**不採用と確定した指標**：prevalence単独（>=2.3で3,529語を捕まえるが ambiguous/cynical/endorse/compassion/treason 等のL2学習者に必要な語を巻き込む——prevalenceもAoAも「ネイティブの習得」を測る指標でL2の難易度とは別軸。#32でzipfが失敗したのと同じ罠の一段深い版）／米国上位姓リスト単独（angel/archer/bacon/berry/cherry/fox/grace 等の普通名詞が大半のため容疑スコア専用）。**実測結果**：固有名詞疑い 6.9%→0.0%、AoA<=8歳 12.5%→0.0%、EJDict非収録 24.0%→13.3%、AoAの最小値 1.6歳(momma)→8.0歳、中央値 10.8→11.7歳。**IDは詰め直さない**——`cardId` は並び位置ベースでDexieのprogress主キーなので、emit再実行だと既存ユーザーの進捗が別単語に付け替わる（上級漢字コースで740件中664件の付け替わりを実測済み）。よって public/data を直接 in-place 編集し欠番のまま残す。emit側は凍結リスト方式をやめ `raw/en-10-30k-easy-removed.json` を読む方式に変更。**ライセンス**：AoA(Kuperman)・prevalence(Brysbaert) は CRR の CC BY-NC-SA 4.0（非商用）。Kohei判断で採用——ビルド時フィルタとしてのみ参照し、`pipeline/raw/` は .gitignore 済みなのでデータ自体も派生物も public リポジトリには入らない（成果物は除外した英単語のリストのみ＝事実の列挙）。米国国勢調査の姓リストはパブリックドメイン。新規: `fetch_difficulty_norms.py` / `analyze_easy_words_en.py` / `make_easy_review_batches.py` / `filter_easy_words_en_10_30k.py` / `report_easy_words_en.py`。build・typecheck・Playwright目視確認済み。**表示名**：Kohei判断で `English 10,000 → 30,000` から **`English 10,000 → 22,000`** に変更（#33で固定値にした経緯はあるが、今回の除去で実質の既知語底面が3,772→約10,000語になり「10,000スタート」が初めて実態と一致したため、終点も実態に合わせた。30,000のままだと表示と実態が8,000語分ずれる）。`emit_en_10_30k.py` の DISPLAY_END・`meta.json`（title/band.to）・`courseRegistry.ts` の3か所を更新。**未対応**: 容疑スコア2の2,610語は未レビュー（apps/stats/physic/android 等の残存はここ） | 2026-08-02 |
| 35 | **コースB（tl-0-2k）にIPA発音記号を追加（`reading`アクセント表記と併記）** | Kohei依頼：「タガログ語コースに発音記号をつけてほしい」。既存の`reading`（アクセント位置表記のみ）に加え、新規フィールド`ipa`を追加し併記表示する方式をKohei選択。**IPAは既にビルド中間データ`pipeline/raw/tl-skeleton.json`に93.2%（1,866/2,002語）分取得済みだったが、最終出力段階（`build_tl_0_2k.py`/`emit.py`）で配線されず捨てられていたと判明**——新規AI生成はほぼ不要で配線を直すだけで大部分をカバーできた。残り136語（6.8%・接辞付き活用形が中心）はWorkflow（生成→2人独立検証→不一致は3人目が裁定。合意116語／裁定20語）で埋め、全2,000語をカバー。**重大な発見**：`build_tl_0_2k.py`をフル再実行したところ、想定外の新語「eksena」が2001語目として紛れ込み、既存カード4件のgloss/例文まで変化した（うち1件は実在しなさそうな単語を含む例文に化けていた）。**根本原因は`pipeline/raw/tl-content-batches`（gloss・例文の中間データ）が出荷済み`public/data/courses/tl-0-2k/`と既に非同期になっていたこと**——`pipeline/raw/`が`.gitignore`対象（バージョン管理外）のため、中間データが出荷後に更新されても検知できない構造上の欠陥。今回のIPA変更が原因ではなく、以前から存在していた問題が再ビルドで表面化した。**対応**：フルビルドは使わず、既存の出荷済みJSON（2,000語）に対し`(headword, reading)`キーで`ipa`フィールドだけを追加する安全なパッチ方式を採用し、gloss/examples/カテゴリ/cardIdは一切変更せず出荷。`build_tl_0_2k.py`には将来の保険として「2,000語キャップ」（定員超過分を切り捨てログ出力）を追加済み。UI側は`StudyGrid.tsx`/`FocusSheet.tsx`/`AllWords.tsx`/`WordSearch.tsx`/`CoursePreviewScreen.tsx`の5箇所に`ipa`併記を追加し、検証中に`.word-search-item`で長いIPA表記による横はみ出し（17px）を発見・`.word-search-reading`のflex/overflow-wrap設定で修正。typecheck・テスト91件・ブラウザ実動作確認済み。**未対応**：`pipeline/raw/tl-content-batches`と出荷物の非同期自体は今回解消していない——将来このコースをフルビルドし直す場合は先に中間データを出荷物と照合し直す必要がある | 2026-08-05 |
| 36 | **新コース「タガログ語 日常フレーズ集」（`tl-phrases-daily`）の計画確定** | Kohei依頼：「タガログ語で日常的に頻繁に使えるフレーズのコースを作りたい」（訳は日本語）。planner（Opus）による実装計画をKoheiと協議し確定。**規模＝400〜500フレーズ**（Kohei選択・標準版）。**カテゴリ＝10カテゴリで日常全般をカバーしつつ職場・実務を厚めに**（Kohei選択）——挨拶45／感情表現50／職場65／面接（新設）25／食事40／買い物40／交通40／数字時間お金35／病院緊急時（新設）35／トラブル対応（新設）40／役所手続き（新設）35＝計450。**話者はKohei（日本人HR）がフィリピン人スタッフ・候補者に話す前提**（Kohei判断・逆だと職場・面接カテゴリの主語敬語方向が全作り直しになるため必須確定事項）。**職場・会議系はTaglish（英語混じり）を許容**（Kohei判断・フィリピンの実際の職場コードスイッチに合わせる）。技術方針は推奨案で確定：①IPAはv1で付けない（アクセント表記`reading`のみ）②cardIdは内容キーでなく不変の`slotId`（`idKey`をcard_id.pyに後方互換追加）③CourseTypeに`'phrase'`を新設④データ生成はAI生成主軸・Tatoeba補助（**実測でTatoebaタガログ語コーパスは「英語文の翻訳」であり定番フレーズの完全一致率が22件中4件のみと判明**・フレーズブックとしては使えない）⑤品質担保は#35と同じ「生成→観点分割2検証→裁定」Workflowパターンを流用⑥見出し語UIは実測フィット（`useFitText`）に切替（tl-0-2k例文2,332件を見出し語長として測定した結果、現行の静的閾値では64.5%が最下段21pxに張り付くと判明・単語コースの既存パスは変更しない）。**#35で発覚した「pipeline/raw非同期」問題への対策として、新コースのソースは`pipeline/data/tl-phrases-daily/`にgit管理で置き、ビルドスクリプトに`--check`冪等性モードを実装する方針を明記**。詳細設計・カテゴリ内訳・フェーズ分解（Phase 0〜6・Koheiレビューは2箇所のみ）・リスク評価は `docs/tl-phrases-plan.md` を正とする。**同一セッション内でPhase 0〜6を完走・出荷まで完了**：①card_id.pyにidKey対応を後方互換追加、既存9コース＋新コースの計10コースでtest_id_stability.py全緑確認（headword/reading書き換え後も同一cardId維持を実証）②450スロット表をWorkflow（11カテゴリ並列）で生成、重複疑いペア0件③20件パイロットで実機（ブラウザJS実測）検証しKoheiレビュー通過④450件本生成（Workflow 15バッチ×30件）、過不足0件⑤機械検証（slotId整合性・po/ho含有・語彙ゲート・Tatoeba attestation）で450件中446件即パス、残り4件はTaglish由来の想定内⑥AI検証（言語面・場面面の独立2観点×15バッチ＋裁定）で102件に指摘、96件revise・6件keep_as_isを適用⑦`build_tl_phrases_daily.py`（純関数・`--check`冪等性モード実装）で出荷、2回連続`--check`で差分ゼロを実証⑧UI改修：`CourseType`に`'phrase'`追加、見出し語を`useFitText`ベースの`FitHeadword`に切替（フレーズコースのみ・単語コースの既存パスは不変）、`--tile-h`をCSS変数化しフレーズコースはデスクトップ232px/モバイル250pxに拡張、`.tile-reading`を折り返し可に統一、`.focus-sheet`に`max-height:90vh`+スクロール追加、`.word-search-word`のflex-shrink許可（168文字級の見出し語で発生した横はみ出しを解消）⑨320/375/620/900/1366pxで横スクロール・見出し語クリップともに実測ゼロを確認、`npm run build`成功。**未対応**：CoursePreviewScreen（未割当コースのプレビュー画面）は`phrase`クラス付与のみの最小対応（見出し語の実測フィット化はしていない）。450件中最長168文字（transport-28）はStudy画面での実地確認を省略し、`useFitText`のoverflowing判定によるフェード処理（`.tile-hw-fit-box.clipped`）の設計保証のみで担保。`wordsStarted`等の進捗メーター文言は「words」のまま（コース種別で「phrases」に出し分ける仕組みは無く、既存の共通ラベルを変更するとkana等の回帰確認が要るため見送り） | 2026-08-05 |
| 37 | **tl-phrases-dailyを全450件1文化＋音声読み上げボタンを追加** | Kohei依頼：出荷直後に2点フィードバック。①「フレーズはすべて一フレーズのみに」②「このコースだけ音声ファイルをつけられないか」。**①**：450件中179件（39.8%）が2〜3文構成だったと判明（例:「挨拶＋自己紹介」を1カードにまとめていた）。Kohei判断で「1文に短縮」（分割して枚数を増やす案は不採用）。Workflow（6バッチ）で、各スロットのintent_jaに最も合致する中心的な1文を残す形で短縮し、reading/gloss/root/examplesも整合させて作り直した。cardIdはidKeyベース（#36で対応済み）のため全件同一IDを維持。短縮後は最長158文字（元は複数文で168文字）・平均69.7文字に減少。**②**：音声技術を調査した結果、**Cloudflare Workers AIのTTSモデル（melotts, deepgram/aura）はタガログ語に対応していないと判明**（対応言語ドキュメントに記載なし）。Google Cloud Text-to-SpeechはFilipino(fil-PH)に正式対応（無料枠内で収まる見込み）だが、Kohei判断で**まずコストゼロのブラウザ内蔵音声合成（Web Speech API）を試す**方針に決定。`src/audio/tts.ts`を新規実装——`speechSynthesis.getVoices()`でtl-/fil-系の音声を実行時に探し（BCP47完全一致判定。前方一致だと`tlh`＝クリンゴン語を誤検出するバグを自己レビューで発見し修正）、**見つかった場合だけ**タイル裏面・FocusSheetに🔊再生ボタンを表示する設計（対応端末が無ければボタンごと出さない・「押しても鳴らない」を作らない）。この開発環境（ヘッドレスChrome）にはタガログ語音声が無く実地確認はできなかったため、**Kohei本人の端末での動作確認が必要**（Android+Chromeなら対応の可能性、Windows/iOSは非対応の可能性が高いと推測）。対応音声が無い場合の代替（Google Cloud TTS等）は次の判断ログで検討 | 2026-08-05 |
| 38 | **音声読み上げ機能の結論：Web Speech API方式は非対応・Google Cloud TTSへの切替は今回見送り** | Kohei実機（自身の端末）で確認した結果、🔊ボタンが表示されなかった＝tl-/fil-系の音声が端末に無いと判明（#37で想定していたシナリオが実際に発生）。代替案として提示したGoogle Cloud TTS（Filipino正式対応・無料枠内で収まる見込み）への切替を確認したところ、**Kohei判断で見送り**（アカウント作成等の追加作業が要るため）。**音声機能は当面なし・フレーズコースはテキストのみで運用**。`src/audio/tts.ts`・`useTagalogVoice.ts`・タイル裏面/FocusSheetの🔊ボタン実装はコードとして残す（対応端末が無ければ何も表示されないだけで実害がなく、追加コストもゼロのため削除しない。将来的に対応端末が増える／Google Cloud TTSに進む判断をした場合にすぐ再利用できる） | 2026-08-05 |

## 9. 主要リスクと対策

| リスク | 対策 |
|---|---|
| 10k-30k 帯の例文カバレッジの崖（Tatoeba では過半ゼロ） | JESC 第2源＋LLM 生成（AI生成フラグ）の3段構え |
| wordfreq 頻度/レンマ化品質（rank20k超はノイズ増。日本語も BCCWJ→wordfreq/OPUS に変更） | Phase 0 で実測してから帯上限を確定 |
| タガログ語の 2k→3k 域はデータが薄い | McFarland 骨格＋コーパス統合。3kに届かなければ無理に水増しせず届いた語数で出す（判断ログ#19） |
| メンバー配布時の学習継続 | ワークロード数字を最初に見せる＋ストリーク＋JLPTバッジ＋Teams マイニングで業務直結 |
| 貼り付けマイニングが面倒で使われない | Phase 6 のクイック辞書引き（PWA常駐）で摩擦低減、それでも不満なら Graph API 自動化 |
| 進捗データの消失（ローカルのみ・ブラウザ消去で消える） | 手動 JSON エクスポート/インポート導線を目立たせ、初回オンボーディングで案内。サーバー同期はしない方針 |
| public 公開で再配布不可データを漏らす | データ層を最初から再配布可能のみに限定（BCCWJ・jpdb 不使用）。public リポジトリにシークレットを置かない |
| **コンテンツ品質＝人手ネイティブレビュー無し（判断ログ#18）** | タガログ語グロス・英語グロス・例文・コーチ文の品質上限はAI自身の多言語能力に依存し、人手検証では捕まる誤りが残る可能性がある。生成→別AIによる敵対的検証（相互検証）を必須プロセスとし、明らかな誤生成の検出だけでも機械的に行う。品質問題が使用中に見つかった場合の訂正導線（フィードバック）を用意する |
| **カテゴリ分類・コーチ文に自動生成スクリプトが存在しない** | コースCの208文・28カテゴリはAI著者セッションの一回限りの成果物で、生成コストは**コースごとに毎回まるごと発生**する（自動化されていない）。各コースのタスクリスト（2.5/3.5/4.5/5.5）に明示のLサイズ工数として計上済み。過小評価しない |
| **共通基盤（Phase 1.5）を後回しにした場合の手戻り** | コース切替UI・引用元表示・カバレッジ計算・i18nを2コース目以降に個別対応すると、後から3コース分やり直す事態になり得る。判断ログ#15によりPhase 1.5として先に一括対応する方針を採用済み |
| ja-0-3k のグロス言語がパイプラインスクリプト・PLAN.mdと食い違ったまま | 判断ログ#16でタガログ語を正と確定。Phase 3.1 で `build_ja_0_3k.py` 系のスクリプト自体にタガログ語訳ステップを正式に組み込み、以後の手翻訳（一回限りの手作業）を無くす |
| PWAが全コースのJSONを全訪問者に配信（vite.config.ts の既知の未解決課題） | Phase 1.5.8 でグロブ全指定からruntimeCachingへ移行。5コース分になった時点で影響が実害化するため後回しにしない |
| 【Phase0.4実測】コースE（10k→30k）は安全な頻度シグナルだけでは20,000語中65%（12,935語）しか埋まらない。無フィルタ化すると同音異義語の頻度誤付与（例:「画架」が「画家」の頻度を借用）や文法語混入が実害として発生する | **判断ログ#22で確定**: 範囲を実質「ja-10k-23k」相当に縮小して出荷（品質妥協・追加コーパス投入は不採用） |
| 【Phase0.3実測】英語コースA例文の英日対訳ペア充足率が52.1%しかなく、しかも高頻度帯(25k-30k)ほど悪化する崖がある(37.8%まで低下)。JESCでの補完実績は未測定 | Phase 5着手前にTatoeba欠落分(273語)へのJESCカバレッジを追加スパイクで実測し、最終的なLLM生成必要量を確定してから着手する |
| 【Phase0.2実測】タガログ語グロス生成の複数AI相互検証（判断ログ#18）が、実際にはこの実行環境で外部LLM APIに接続する手段がなく実施不可だった | 自己再点検（1つのAIが自分の生成を疑って再確認する）で代替。Phase 2の本番生成前に、別セッション/別モデルでの独立検証を最低1回挟むことを推奨（未解決） |
| 【Phase2実測・判断ログ#23】コーチ文生成で `words[]`（既習語ゲート配列）の宣言漏れが170件中93件と高頻度で発生した。生成担当のコンテキストでは「文に使った語を全部列挙する」という機械的作業が抜け漏れやすい | 生成後に必ず独立コンテキストでの機械的検証（形態素トークナイズ→全語がwordsに宣言されているか）を挟む。D/E/Aのコーチ文生成でも同じ検証ステップを標準工程に組み込むこと |
| 並列バッチ処理で出力ファイル名が衝突・混乱した（Phase2の8バッチ並列コンテンツ生成で発生。batch-01用エージェントが命名を自己判断で変更） | マージ担当がheadword単独でなくfrequencyRank複合キーで結合したため実害は出なかったが、今後の並列バッチ処理では**各エージェントへの出力ファイル名を担当範囲込みで固定文字列として明示する**（"machine分割してよい"という曖昧な指示を避ける）ことを次回以降のワークフロー設計で徹底する |

## 10. 差別化メモ（将来の外販オプション・参考）

- タガログ語×頻度順SRSの本格アプリは実質空白市場（本格派は Clozemaster/LingQ のみ・日本語UIはほぼ皆無）。コースB は将来外販候補。**データ層は最初から再配布クリーン（BCCWJ・jpdb 不使用）なので、外販時もデータ差し替えが不要**（独自例文の権利だけ確認すれば商用移行できる）
- メンバー用日本語コース（JLPTバッジ付き）は比国の就労日本語需要に直結＝社内 HR 施策としても価値。Accenture 内での横展開余地

## 付録: 調査アーカイブ

- 全調査結果（データセット詳細・ライセンス出典URL・競合分析・学習方式の参考文献）: セッション scratchpad の `wsiobwp6p.output`（要点は本計画に反映済み）
- 主要参考: FSRS 公式ベンチマーク／Meara & Buxton 1987（Yes/No テスト）／Nation 2006（語彙被覆率）／Schmitt 2014（size vs depth）／Brysbaert 2016（母語話者≈42k レンマ）
